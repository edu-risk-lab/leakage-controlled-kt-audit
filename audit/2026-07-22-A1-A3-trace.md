# Truy vết A1 + A3 — gửi PGS (draft nội bộ)

**Ngày:** 2026-07-22

## A1 — Lệch baseline injection (Table S18 vs bảng chính)

| Model | S18 clean (fold 0) | Primary train_only (3-fold mean) | Δ |
|---|---:|---:|---:|
| simpleKT | 0.850 | 0.875 | −0.025 |
| GKT | 0.810 | 0.834 | −0.024 |
| GIKT | 0.852 | 0.878 | −0.026 |

**Nguyên nhân (2026-07-22, rà soát đầy đủ):**
1. Table S18 **tạo từ placeholder** (`6a0b09fc` + `create_fake_tables.py`); **không có** cache/log `inject05/20` trong repo/git.
2. S17 (`leak_injection`) **thật** — graph metrics regenerate khớp.
3. Cột **0% clean** đã thay bằng fold-0 `train_only` thật (`collect_injection_auc.py`); cột **5%/20%** = `pending` (cần GPU).

| Model | S18 cũ (placeholder) | Clean thật (fold 0) | Primary 3-fold mean |
|---|---:|---:|---:|
| simpleKT | 0.850 | **0.8744** | 0.875 |
| GKT | 0.810 | **0.8346** | 0.834 |
| GIKT | 0.852 | **0.8776** | 0.878 |

**Hành động:** GPU rerun 6 job (inject05/20 × 3 model). Chi tiết: `audit/2026-07-22-A1-injection-data-audit.md`.

---

## A3 — Junyi very_cold artefact

**Hiện tượng:** Table S13 — mọi model AUC = 0.813 ± 0.324 (trùng 3 chữ số thập phân).

**Kiểm chứng từ `cold_start_metrics.csv` (Junyi, stratum=very_cold):**

| Fold | n | AUC (mọi model) |
|---:|---:|---|
| 0 | 19 | 1.000 |
| 1 | 48 | 0.4390625 |
| 2 | 56 | 1.000 |

Mean = (1 + 0.4390625 + 1) / 3 = **0.8130208…** — trùng cho mọi model vì AUC **giống hệt theo fold**, không phải do model khác nhau.

**Giải thích:** Suy biến thống kê (ít cặp discordant; fold 0 n=19 → AUC=1.0). Không phải insight kiến trúc.

**Sửa đã làm:** Suppress ô AUC không đáng tin trong `generate_cold_start_comparison.py`; pipeline mới đếm discordant pairs (`src/cold_start_report.py`). Cần rerun cold-start với full predictions (không cap 5000) để số mới.
