\begin{table}[t]
\centering
\caption{Leakage exposure bound on XES3G5M (GKT, fold~0). Raw $\delta$ is leaked edges over retained train-only edges (Table~\ref{tab:m4-qk-census}); $\delta^{\mathrm{w}}$ is the weight mass pooling moves. Bounds are $s\cdot\delta$ and $s\cdot\delta^{\mathrm{w}}$ with $s$ the DDR slope, from a CPU census and one reliance probe. Both hold on all four trained cells. A bound is informative only where it exceeds the measured floor $\sigma$ (Section~\ref{sec:exposure}); all four $|\Delta\text{AUC}|$ sit at or below that floor, so the table does not resolve the two predictors at this budget. Remaining census cells are in Supplementary Table~S25.}
\label{tab:leakage-exposure}
\footnotesize
\setlength{\tabcolsep}{3pt}
\begin{tabularx}{\linewidth}{@{} >{\RaggedRight\arraybackslash}X *{5}{>{\centering\arraybackslash}c} @{}}
\toprule
Cell & $\delta$ & $s\cdot\delta$ & $\delta^{\mathrm{w}}$ & $s\cdot\delta^{\mathrm{w}}$ & $|\Delta\text{AUC}|$ \\
\midrule
Published default & 0.089 & 0.0075 & 0.035 & 0.0030 & 0.0006 \\
Review open $q$ & 0.042 & 0.0036 & 0.022 & 0.0018 & 0.0015 \\
$k{=}20$ lift & 0.073 & 0.0061 & 0.019 & 0.0016 & 0.0005 \\
$k{=}\infty$ lift & 0.132 & 0.0111 & 0.022 & 0.0018 & 0.0004 \\
\bottomrule
\end{tabularx}
\end{table}
